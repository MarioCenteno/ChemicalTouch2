using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ElementBox : MonoBehaviour
{
    //Element Box - Contém o atomo a que faz referência o elemento selecionado da tabela


    // Start is called before the first frame update

    public GameObject atom;


    void Start()
    {

        
    }

    // Update is called once per frame

    public GameObject getAtom()
    {
        return atom;
    }

 // Adicionar possível variável de retorno do tipo de atomo a devolver



//Ver classe AtomV2
}
